{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    },
    "allowJs": true, // Allows JavaScript files
    "resolveJsonModule": true, // For importing JSON if needed
    "jsx": "react-jsx", // For React JSX files
    "strict": true, // Enable all strict type-checking options
    "esModuleInterop": true, // Enables interoperability between CommonJS and ES Modules
    "skipLibCheck": true, // Skip type checking of declaration files
    "noEmit": true, // Keep TS language service in type-check mode only
    "forceConsistentCasingInFileNames": true // Prevent cross-OS path casing issues
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules"]
}
